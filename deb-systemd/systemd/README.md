# Systemd Service File

This directory contains `coredns11.service` which is the systemd unit file for the `coredns11` custom package.

The package installs to `/opt/coredns` and runs as the `coredns` user.

Files:
- `coredns11.service`: The systemd unit file.
